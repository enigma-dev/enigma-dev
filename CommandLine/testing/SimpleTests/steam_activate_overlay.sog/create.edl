
var is_initialized = steam_initialised();

gtest_assert_eq(is_initialized,  true);

steam_activate_overlay();

var is_overlay_enabled = steam_is_overlay_enabled();

gtest_assert_eq(is_overlay_enabled,  true);
