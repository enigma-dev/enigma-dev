
struct PathPoint {
  x:int;
  y:int;
  speed:int;
}

table Path {
  name:string;
  id:int;

  smooth:bool;
  closed:bool;
  precision:int;

  //background_room_id:int; //This was used by LGM rather than ENIGMA; it doesn't need to be here.
  snap_x:int; //TODO: Find out if these are needed and decide on keeping them
  snap_y:int;

  points:[PathPoint];
}

root_type Path;
