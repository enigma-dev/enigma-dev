//vector<int> tv;
//map<int,int> tm;
scr_fly(('0')+string(global.time2)); 
if zmin-zzmin < 7 then { zmin -= spd1;/* spd = -spd1;*/ } else { zmin -= spd2;/* spd = -spd2;*/ } 

string ts;
vector<int> a;
map b;
map<int> c;
map<int,int> d;
with all
az = bz.length();
