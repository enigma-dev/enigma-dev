set(GENERAL_PLATFORM_LIB "general_platform_lib")
set(GENERAL_PLATFORM_LIB_DESCRIPTION "General Plaform")

project(${GENERAL_PLATFORM_LIB} DESCRIPTION ${GENERAL_PLATFORM_LIB_DESCRIPTION})

if (TARGET-PLATFORM MATCHES ANDROID-PLATFROM)
    file(GLOB_RECURSE GENERAL_PLATFORM_LIB_SOURCES 
        ../SDL/fileio/fileio.cpp)
else
    file(GLOB_RECURSE GENERAL_PLATFORM_LIB_SOURCES 
        ../General/fileio/fileio.cpp)
endif()

add_library(${GENERAL_PLATFORM_LIB} ${GENERAL_PLATFORM_LIB_SOURCES})
