set(ANDROID_PLATFORM_LIB "android_platform_lib")
set(ANDROID_PLATFORM_LIB_DESCRIPTION "Android Plaform")

project(${ANDROID_PLATFORM_LIB} DESCRIPTION ${ANDROID_PLATFORM_LIB_DESCRIPTION})

file(GLOB_RECURSE ANDROID_PLATFORM_LIB_SOURCES *.cpp)

add_library(${ANDROID_PLATFORM_LIB} ${ANDROID_PLATFORM_LIB_SOURCES})
